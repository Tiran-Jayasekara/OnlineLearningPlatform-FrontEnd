import React from 'react'

const AddButton = ({ name }) => {
    return (
        <div className='p-2 bg-green-600 w-fit font-semibold text-white rounded-md'>{name}</div>
    )
}
export default AddButton